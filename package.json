{
  "scripts": {
    "start": "run-p watch server",
    "sass": "sass scss:style",
    "watch": "npm run sass -- --watch",
    "server": "live-server"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.52.1"
  }
}